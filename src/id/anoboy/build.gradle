ext {
    extName = 'Anoboy'
    extClass = '.Anoboy'
    extVersionCode = 5
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(":lib:cloudflare-interceptor"))
    implementation(project(":lib:anoboy-extractor"))
    implementation(project(":lib:playlist-utils"))
}
